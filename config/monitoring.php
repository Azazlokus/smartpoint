<?php

declare(strict_types=1);
use App\Adapters\MockAdapter;

return [
    /*
    |--------------------------------------------------------------------------
    | Зарегистрированные адаптеры источников
    |--------------------------------------------------------------------------
    |
    | Карта slug ресурса → класс адаптера. Чтобы добавить новый источник —
    | достаточно зарегистрировать его здесь, код трогать не нужно.
    |
    | Формат: 'slug' => AdapterClass::class
    |
    */
    'adapters' => [
        'mock' => MockAdapter::class,
    ],
];
